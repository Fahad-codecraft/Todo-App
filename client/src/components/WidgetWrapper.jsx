import { Box } from "@mui/material";
import { styled } from "@mui/system";

const WidgetWrapper = styled(Box)(() => ({
  padding: "1rem 1rem 1rem 1rem",  backgroundColor: "#21212b",
  borderRadius: "0.75rem",
}));

export default WidgetWrapper;
