import { Box } from "@mui/material";
import { styled } from "@mui/system";

const TodosWrapper = styled(Box)(() => ({
  width: "55%",
  padding: "0.5rem 0.5rem 0.5rem 0.5rem",  backgroundColor: "#21212b",
  borderRadius: "0.75rem",
  margin: "0.5rem",
}));

export default TodosWrapper;
